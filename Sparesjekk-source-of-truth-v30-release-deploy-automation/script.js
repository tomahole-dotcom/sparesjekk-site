const $=s=>document.querySelector(s);const parseNum=v=>parseFloat(String(v).replace(/\s/g,'').replace(',','.').replace(/[^0-9.]/g,''))||0;const kr=n=>new Intl.NumberFormat('nb-NO',{maximumFractionDigits:0}).format(n)+' kr';function update(){const loan=parseNum($('#loan')?.value),rate=parseNum($('#rate')?.value),next=parseNum($('#newrate')?.value),saving=Math.max(0,loan*(rate-next)/100);if($('#saving'))$('#saving').textContent=kr(saving);if($('#monthly'))$('#monthly').textContent='ca. '+kr(saving/12)+' per måned'}const loan=$('#loan');if(loan){loan.addEventListener('focus',()=>loan.value=String(parseNum(loan.value)));loan.addEventListener('input',update);loan.addEventListener('blur',()=>{loan.value=new Intl.NumberFormat('nb-NO',{maximumFractionDigits:0}).format(parseNum(loan.value));update()})}['#rate','#newrate'].forEach(s=>{const el=$(s);if(!el)return;el.addEventListener('input',update);el.addEventListener('focus',()=>el.value=String(parseNum(el.value)).replace('.',','));el.addEventListener('blur',()=>{el.value=parseNum(el.value).toLocaleString('nb-NO',{minimumFractionDigits:2,maximumFractionDigits:2});update()})});update();
