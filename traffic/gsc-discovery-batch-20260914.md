# GSC discovery batch – 14 September 2026

Purpose: improve crawl/discovery paths without changing protected title/meta/H1/canonical fields.

## Verified contextual coverage

- `rentegap-indeks.html` is linked contextually from `rentemote-hva-betyr-det.html`.
- `belaningsgrad-sjekk.html` is linked prominently from `guide-belaningsgrad.html`.
- `gjeldsmiks-sjekk.html` and `refinansiering-kalkulator.html` are linked prominently from `refinansiere-forbruksgjeld.html` and the forbrukslån hub.
- `kredittkort-match.html` is linked prominently from `kredittkort.html`.
- `bankbytte-break-even.html` and `min-rente-vs-markedet.html` are linked prominently from `bytte-bank-boliglan-komplett.html`.
- `omstartslan.html` now has an additional contextual link from `omstartslan-guider.html`.
- `sammenligne-forbrukslan.html` now receives clearer contextual authority from `forbrukslan.html` and `forbrukslan-komplett-guide.html` while `effektiv-rente-forbrukslan.html` keeps its existing role.

## Deliberately not changed

- No protected SEO fields were altered.
- No extra sitewide/nav link spam was added.
- No duplicate pages were created.
- `problemloser.html` remains a separate follow-up item because a relevant contextual source page should be chosen before adding an inbound link.

## Indexing status context

The nine priority URLs were manually submitted in Google Search Console on 14 September 2026. The sitemap was then resubmitted and Google fetched the current 104-URL sitemap with zero warnings and zero errors. This batch is intended to reinforce normal crawl paths rather than rely on submission alone.
